export type User = {
    id?: number
    name: string
    email: string
    password: string
    role: string
    phone?: string
}

export type extUser = {
    id: number
    name: string
    email: string
    role: string
    phone?: string
}

